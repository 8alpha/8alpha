import React from "react";

import Layout from "../components/layout";
import {
  SectionContainer,
  SectionHeading,
  SectionParagraph,
} from "../components/styled";

const Success = () => (
  <Layout>
    <SectionContainer>
      <SectionHeading>Success/Thank You Page</SectionHeading>
      <SectionParagraph>Thank you for contacting us</SectionParagraph>
    </SectionContainer>
  </Layout>
);

export default Success;
